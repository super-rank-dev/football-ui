// Importing the material library from Flutter
import 'package:flutter/material.dart';

/// A class to define color constants used in the app.
class AppColors {
  /// The text color used throughout the app.
  static const textColor = Colors.white;

  /// A grey color used for various UI elements.
  static const greyColor = Colors.grey;
}

// Example Usage:
// You can use these colors in your widgets like this:
// Text('Hello', style: TextStyle(color: AppColors.textColor)),
// Container(color: AppColors.greyColor, child: ...),
